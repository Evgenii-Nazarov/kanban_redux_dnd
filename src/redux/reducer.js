const initialState = {
    cards: [],
    columns: []
}

const kanban = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export default kanban;